% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ctfidf_topics.R
\name{plot_ctfidf_topics}
\alias{plot_ctfidf_topics}
\title{plot_ctfidf_topics}
\usage{
plot_ctfidf_topics(
  ctfidf_df,
  topics = NULL,
  n_words = 10,
  palette = "Set2",
  facet = FALSE
)
}
\arguments{
\item{ctfidf_df}{A dataframe produced by \code{compute_ctfidf()}, containing
the columns \code{topic}, \code{word}, and \code{score}.}

\item{topics}{A vector of topic IDs to plot. Defaults to \code{NULL}, which
plots all available topics.}

\item{n_words}{Number of top words (ranked by c-TF-IDF score) to show per
topic. Default is 10.}

\item{palette}{A ColorBrewer palette name used to color the bars.
Default is \code{"Set2"}.}

\item{facet}{Logical; if \code{TRUE}, produces a faceted plot with one panel
per topic. If \code{FALSE}, topics are plotted together. Default is FALSE.}
}
\value{
A ggplot2 object.
}
\description{
This function visualizes class-based TF-IDF (c-TF-IDF) topic representations
by creating bar charts of the top words per topic. It can display a single
topic, several topics, or all topics contained in the c-TF-IDF table.
}
\examples{
\dontrun{
# Assuming `ctfidf_df` is output from compute_ctfidf()
plot_ctfidf_topics(ctfidf_df, topics = 1, n_words = 10)
plot_ctfidf_topics(ctfidf_df, facet = TRUE)
}
}
