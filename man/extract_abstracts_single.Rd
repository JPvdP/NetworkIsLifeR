% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_abstracts_single.R
\name{extract_abstracts_single}
\alias{extract_abstracts_single}
\title{Extract abstracts from a single Lens.org JSONL/JSONL.GZ export}
\usage{
extract_abstracts_single(file_path)
}
\arguments{
\item{file_path}{A path to a single \code{.jsonl} or \code{.jsonl.gz} file.}
}
\value{
A data frame with columns:
\describe{
\item{lens_id}{Character. Lens patent identifier.}
\item{abstract}{Character. Abstract text.}
\item{year}{Integer. Publication year if available, otherwise \code{NA}.}
}
}
\description{
Internal helper used by \code{\link[=extract_abstract_publications_lens]{extract_abstract_publications_lens()}} to parse one \code{.jsonl} or
\code{.jsonl.gz} file and return a data frame of \verb{(lens_id, abstract, year)}.
}
\details{
The function reads the file line-by-line, attempts to parse each line as JSON,
drops malformed records, extracts the abstract and publication year (from
\code{year_published} or the year component of \code{date_published}), and removes rows
without an abstract.

This is an internal function and is not exported. It is intended to be called
by \code{\link[=extract_abstract_publications_lens]{extract_abstract_publications_lens()}} for each file encountered.
}
\examples{
\dontrun{
# Typically not called directly by users:
df <- extract_abstracts_single("data/lens_patents.jsonl.gz")
}
}
\keyword{internal}
