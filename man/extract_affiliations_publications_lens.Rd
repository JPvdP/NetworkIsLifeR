% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_affiliations_publications_lens.R
\name{extract_affiliations_publications_lens}
\alias{extract_affiliations_publications_lens}
\title{Extract publication affiliations from Lens.org JSONL/JSONL.GZ exports}
\usage{
extract_affiliations_publications_lens(file_path, expand_authors = TRUE)
}
\arguments{
\item{file_path}{A path to a \code{.jsonl} / \code{.jsonl.gz} file, or a directory
containing one or more such files.}

\item{expand_authors}{Logical. If \code{TRUE}, returns one row per
author–affiliation combination (as produced by the underlying single-file
parser). If \code{FALSE}, returns the less-expanded structure defined by
\code{extract_affiliations_single()}.}
}
\value{
A data frame (typically a tibble) containing affiliation information.
The exact columns depend on \code{extract_affiliations_single()} and the Lens
export schema.
}
\description{
Read a single Lens.org publications export file (\code{.jsonl} or \code{.jsonl.gz}) \strong{or}
a directory containing multiple such files, and return a combined data frame
with affiliation information extracted from the records.
}
\details{
When \code{file_path} is a directory, all files matching \verb{\\\\.(jsonl|jsonl\\\\.gz)$}
are processed and row-bound in the order returned by \code{\link[base:list.files]{base::list.files()}}.

This function delegates parsing of each individual file to
\code{extract_affiliations_single()}, which must be available in the package
namespace (commonly as an internal helper).

For directory inputs, the function prints basic progress information to the
console (via \code{\link[base:cat]{base::cat()}}).
}
\examples{
\dontrun{
# Single file
aff <- extract_affiliations_publications_lens(
  "data/lens_publications.jsonl.gz",
  expand_authors = TRUE
)

# Directory with many exports
aff_all <- extract_affiliations_publications_lens(
  "data/lens_publications_exports/",
  expand_authors = TRUE
)
}

}
\seealso{
\code{extract_affiliations_single()} (internal helper used per file).
}
