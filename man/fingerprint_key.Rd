% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint_key.R
\name{fingerprint_key}
\alias{fingerprint_key}
\title{Compute the fingerprint key for a single string}
\usage{
fingerprint_key(s)
}
\arguments{
\item{s}{A single character string. \code{NA} returns \code{NA_character_}.}
}
\value{
A single character string containing the fingerprint key, or
\code{NA_character_} if \code{s} is \code{NA}.
}
\description{
Implements the fingerprint keying algorithm from OpenRefine. The string is
trimmed, lowercased, stripped of diacritics and punctuation, split into
whitespace-delimited tokens, deduplicated, sorted alphabetically, and
rejoined with a single space. Two strings that differ only in case,
diacritics, punctuation, or word order will produce the same key and
therefore belong to the same cluster.
}
\examples{
fingerprint_key("Philips")           # "philips"
fingerprint_key("PHILIPS")           # "philips" — same key
fingerprint_key("Cafe\u0301")        # "cafe"    — diacritics removed
fingerprint_key("Stra\u00DFe")       # "strasse" — ß expanded
fingerprint_key("foo bar foo")       # "bar foo" — deduplication
fingerprint_key("Hello, World!")     # "hello world" — punctuation stripped
fingerprint_key(NA_character_)       # NA

}
\references{
OpenRefine FingerprintKeyer source:
\url{https://github.com/OpenRefine/OpenRefine/blob/master/modules/core/src/main/java/com/google/refine/clustering/binning/FingerprintKeyer.java}
}
\seealso{
\code{\link[=fingerprint_key_vec]{fingerprint_key_vec()}} for a vectorised version,
\code{\link[=fingerprint_clean]{fingerprint_clean()}} to apply clustering to a data frame column.
}
