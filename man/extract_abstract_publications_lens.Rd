% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_abtracts_publications_lens.R
\name{extract_abstract_publications_lens}
\alias{extract_abstract_publications_lens}
\title{Extract patent abstracts from Lens.org JSONL/JSONL.GZ exports}
\usage{
extract_abstract_publications_lens(file_path)
}
\arguments{
\item{file_path}{A path to a \code{.jsonl} / \code{.jsonl.gz} file, or a directory
containing one or more such files.}
}
\value{
A data frame (typically a tibble) with columns:
\describe{
\item{lens_id}{Character. Lens patent identifier.}
\item{abstract}{Character. Abstract text (may be \code{NA} if missing).}
\item{year}{Integer. Year extracted from the record (may be \code{NA} if missing).}
}
}
\description{
Read a single Lens.org patent export file (\code{.jsonl} or \code{.jsonl.gz}) \strong{or}
a directory containing multiple such files, and return a combined data frame
of abstracts keyed by Lens identifier.
}
\details{
When \code{file_path} is a directory, all files matching \verb{\\\\.(jsonl|jsonl\\\\.gz)$}
are processed and row-bound in the order returned by \code{\link[base:list.files]{base::list.files()}}.

This function delegates the actual parsing of an individual file to
\code{extract_abstracts_single()}, which must be available in the package namespace
(commonly as an internal helper).

For directory inputs, the function prints basic progress information to the
console (via \code{\link[base:cat]{base::cat()}}).
}
\examples{
\dontrun{
# Single file
df <- extract_abstract_publications_lens("data/lens_patents.jsonl.gz")

# Directory with many exports
df_all <- extract_abstract_publications_lens("data/lens_exports/")

# Inspect
head(df_all)
}

}
\seealso{
\code{extract_abstracts_single()} (internal helper used per file).
}
