% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_fractional_counts.R
\name{compute_fractional_counts}
\alias{compute_fractional_counts}
\title{Compute fractional counts by region (or any grouping column) within an ID.}
\usage{
compute_fractional_counts(
  df,
  id_col,
  region_col,
  unit_col = NULL,
  by_cols = NULL,
  distinct_unit = TRUE,
  drop_na = TRUE,
  frac_name = "fractional_count",
  n_total_name = "n_unit_total",
  n_region_name = "n_unit_region",
  validate_sum = TRUE
)
}
\arguments{
\item{df}{A data.frame / tibble.}

\item{id_col}{Column name (string) that identifies the item to fractionally allocate
(e.g., publication id like "EID").}

\item{region_col}{Column name (string) that defines the regional/group level
(e.g., "Provincie_DEP").}

\item{unit_col}{Optional column name (string) defining the unit to count within an ID
(e.g., affiliation id/name like "Clean"). If NULL, each row is treated as 1 unit.}

\item{by_cols}{Optional character vector of additional columns to compute fractions within
(e.g., c("Year") to compute per Year x EID).}

\item{distinct_unit}{Logical. If TRUE and unit_col is provided, counts \emph{unique} units
per (by_cols, id, region). (Recommended when unit_col is an affiliation name.)}

\item{drop_na}{Logical. If TRUE, drops rows with NA in any required columns.}

\item{frac_name}{Name of the output fractional count column.}

\item{n_total_name}{Name of the output total-units-per-id column.}

\item{n_region_name}{Name of the output units-per-(id,region) column.}

\item{validate_sum}{Logical. If TRUE, warns when fractional shares don't sum ~1 per ID
(within by_cols, if provided).}
}
\value{
A tibble with columns: by_cols (if any), id_col, region_col,
n_total_name, n_region_name, frac_name.
}
\description{
For each ID (e.g., publication), this computes the share of "units" that fall in each
region/group. The default interpretation is:
}
\details{
\deqn{fraction_{(id, region)} = n_{units \in region} / n_{units \in id}}

Example: if an article (EID) has 4 affiliations from region A and 4 from region B,
then each region gets 4/8 = 0.5 for that EID.
}
\examples{
# Fractional affiliations by province within each publication:
# out <- compute_fractional_counts(
#   df = Scopus_affiliations_NL_NDS,
#   id_col = "EID",
#   region_col = "Provincie_DEP",
#   unit_col = "Clean"
# )

# If you want fractions per Year x EID:
# out_y <- compute_fractional_counts(
#   df = Scopus_affiliations_NL_NDS,
#   id_col = "EID",
#   region_col = "Provincie_DEP",
#   unit_col = "Clean",
#   by_cols = c("Year")
# )

}
