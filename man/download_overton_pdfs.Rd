% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_overton_pdfs.R
\name{download_overton_pdfs}
\alias{download_overton_pdfs}
\title{Download PDF files from an Overton export}
\usage{
download_overton_pdfs(input_path, out_dir, sleep_sec = 0.1, max_tries = 4L)
}
\arguments{
\item{input_path}{\code{character(1)}. Path to the Overton CSV export file.}

\item{out_dir}{\code{character(1)}. Path to the directory where PDFs will be
saved. Created automatically (including parents) if it does not exist.}

\item{sleep_sec}{\code{numeric(1)}. Seconds to wait between requests. Increase
this to be more respectful to remote servers. Default is \code{0.1}.}

\item{max_tries}{\code{integer(1)}. Maximum number of retry attempts per URL.
Default is \code{4}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per unique PDF URL and the
following columns:
\describe{
\item{doc_id}{Document identifier (from \code{document_id}, \code{overton_id},
or row number).}
\item{pdf_url}{The URL that was (or was attempted to be) downloaded.}
\item{file}{Full path to the local file.}
\item{status}{HTTP status code, or \code{NA} on a connection error.}
\item{ok}{\code{TRUE} if the file downloaded successfully or already
existed; \code{FALSE} on failure.}
\item{note}{Human-readable summary of the outcome.}
}
}
\description{
Reads a CSV export from Overton, auto-detects the PDF URL column, and
downloads each unique PDF to a local output directory. Already-downloaded
files are skipped so the function can be safely re-run after interruptions.
}
\examples{
\dontrun{
results <- download_overton_pdfs(
  input_path = "path/to/overton_export.csv",
  out_dir    = "path/to/output_directory"
)

# Inspect failures
failed <- results[!results$ok, ]
print(failed)
}

}
