% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Clean_company_names.R
\name{clean_company_names}
\alias{clean_company_names}
\title{Clean company names using rule-based canonicalization}
\usage{
clean_company_names(
  df,
  company_col = "company",
  rules = NULL,
  overwrite = FALSE,
  original_col = paste0(company_col, "_raw"),
  cleaned_col = paste0(company_col, "_clean"),
  ignore_case = TRUE,
  trim = TRUE,
  return_rule = FALSE
)
}
\arguments{
\item{df}{A data.frame/tibble containing company names.}

\item{company_col}{Character scalar. Column name in \code{df} with company names.}

\item{rules}{Optional rule tibble/data.frame with columns:
\code{priority}, \code{type}, \code{tokens} (list-column), \code{pattern}, \code{canonical}.
If NULL, uses the package ruleset from \code{company_rules.r} (see Details).}

\item{overwrite}{Logical. If TRUE, overwrites \code{company_col} with cleaned names.
The original is still preserved in \code{original_col}.}

\item{original_col}{Character scalar. Column name to store original names.}

\item{cleaned_col}{Character scalar. Column name to store cleaned names when
\code{overwrite = FALSE}.}

\item{ignore_case}{Logical. If TRUE, matching is case-insensitive.}

\item{trim}{Logical. If TRUE, trims whitespace and collapses multiple spaces
in both original and cleaned values (recommended).}

\item{return_rule}{Logical. If TRUE, adds a column \code{rule_index} indicating
which rule row (after ordering) matched each name (NA if none).}
}
\value{
\code{df} with original column preserved and cleaned names added/updated.
}
\description{
Applies a prioritized set of string/regex matching rules to map company names
to canonical names. The original company names are always preserved.
}
\details{
A rule matches if:
\itemize{
\item \code{pattern} is non-NA and \code{grepl(pattern, x)} is TRUE, OR
\item \code{pattern} is NA and \code{tokens} is used:
\itemize{
\item type == "ALL": all token regexes must match
\item type == "ANY": at least one token regex must match
}
}

Rules are applied in ascending \code{priority} order; the first match wins.
}
