% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_affiliations_publications_lens_single.R
\name{extract_affiliations_publications_lens_single}
\alias{extract_affiliations_publications_lens_single}
\title{Extract publication affiliations in a simplified table}
\usage{
extract_affiliations_publications_lens_single(file_path)
}
\arguments{
\item{file_path}{A path to a \code{.jsonl} / \code{.jsonl.gz} file, or a directory
containing one or more such files.}
}
\value{
A data frame (typically a tibble) with columns:
\describe{
\item{lens_id}{Character. Lens publication identifier.}
\item{year}{Integer. Publication year if available, otherwise \code{NA}.}
\item{author_name}{Character. Author display name (constructed when missing).}
\item{affiliation_name}{Character. Affiliation name if present.}
\item{city}{Character. Affiliation city if present.}
\item{country}{Character. Affiliation country if present.}
}
}
\description{
Read a single Lens.org publications export file (\code{.jsonl} or \code{.jsonl.gz}) \strong{or}
a directory containing multiple such files, and return a combined data frame
with affiliations in a reduced set of fields.
}
\details{
When \code{file_path} is a directory, all files matching \verb{\\\\.(jsonl|jsonl\\\\.gz)$}
are processed and row-bound in the order returned by \code{\link[base:list.files]{base::list.files()}}.

This function delegates parsing of each individual file to
\code{extract_affiliations_simple_single()}, which must be available in the package
namespace (commonly as an internal helper).

For directory inputs, the function prints basic progress information to the
console (via \code{\link[base:cat]{base::cat()}}).
}
\examples{
\dontrun{
# Single file
aff <- extract_affiliations_publications_lens_single("data/lens_publications.jsonl.gz")

# Directory with many exports
aff_all <- extract_affiliations_publications_lens_single("data/lens_publications_exports/")
}

}
\seealso{
\code{extract_affiliations_simple_single()} (internal helper used per file).
}
