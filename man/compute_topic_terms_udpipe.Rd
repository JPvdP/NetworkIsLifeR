% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_topic_terms_udpipe.R
\name{compute_topic_terms_udpipe}
\alias{compute_topic_terms_udpipe}
\title{Compute topic representation terms using udpipe + quanteda (R-only)}
\usage{
compute_topic_terms_udpipe(
  data,
  doc_col,
  topic_col,
  text_col,
  ud_model,
  use_lemma = TRUE,
  max_ngram = 3L,
  min_term_freq = 5L,
  allowed_upos = c("NOUN", "PROPN", "ADJ")
)
}
\arguments{
\item{data}{A data.frame with at least three columns: doc id, topic, text.}

\item{doc_col}{Name of the document ID column (unquoted).}

\item{topic_col}{Name of the topic column (unquoted).}

\item{text_col}{Name of the text column (unquoted).}

\item{ud_model}{A loaded udpipe model (see udpipe::udpipe_download_model()
and udpipe::udpipe_load_model()).}

\item{use_lemma}{If TRUE, use udpipe lemmas, otherwise surface tokens.}

\item{max_ngram}{Maximum n-gram length (e.g. 3 for 1-3 grams).}

\item{min_term_freq}{Minimum total frequency of a term in the corpus
to keep it (integer >= 1).}

\item{allowed_upos}{Coarse POS tags to keep before making n-grams
(default: c("NOUN", "PROPN", "ADJ")).}
}
\value{
A data.frame with one row per (topic, term) and columns:
topic, term, tf, tf_norm, tf_idf, c_tf_idf, lift, df_corpus,
tf_corpus, total_tf_topic, n_topics_with_term.
}
\description{
Compute topic representation terms using udpipe + quanteda (R-only)
}
\examples{
\dontrun{
udpipe::udpipe_download_model('english')
model <- udpipe::udpipe_load_model()
compute_topic_terms_udpipe(df, ud_model = model)
}
}
